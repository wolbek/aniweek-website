Create & fill up:
backend/.env
backend/gcp-service-account-key.json
infra/terraform.tfvars
worker/.env

There are .example files (.env.example, gcp-service-account-key.example.json, terraform.tfvars.example) files listing what all is to be added.
